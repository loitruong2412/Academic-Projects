package enums;

/**
 * This enum represents the Countries in the world with the flag that is supported as a feature.
 * This is best used paired with a pick list in the view.
 */
public enum Country {
  FRANCE, SWITZERLAND, GREECE,
}
