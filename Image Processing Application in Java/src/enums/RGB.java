package enums;

/**
 * This is an enum that represents the RGB (Red, Green, Blue color model). This is used to check
 * what color value is being updated with the RBG value.
 */
public enum RGB {
  RED, GREEN, BLUE
}
